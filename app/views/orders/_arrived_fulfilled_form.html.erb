 <% order = @order if @order %>

<% if !order.arrived %>

	<%= form_for order do |f| %>
	  <%= f.hidden_field :arrived, value: true %>
	  <%= f.submit "Mark As Arrived" %>
	<% end %>

<% else %>

	<% if !order.fulfilled %>
	  <%= form_for order do |f| %>
	    <%= f.hidden_field :fulfilled, value: true %>
	    <%= f.submit "Mark As Fulfilled" %>
	  <% end %>
  <% end %>

<% end %>



